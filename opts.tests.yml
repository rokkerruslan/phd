
# WARNING: ALL FIELDS ARE REQUIRED!

# ip/port pair for binding server socket
Addr: ":3001"

# Postgres DSN
DatabaseURL: "postgres://postgres:postgres@localhost:5432/ph_tests?sslmode=disable"

# Used for password hash calculating, no default value. Please set yourself.
# Example bash command for generating:
# $ openssl rand -base64 32
# Minimal len - 32.
GlobalSalt: "asadfaksdfakjshdfkahsfkjahsdflkahdflashdfalh"
BcryptWorkFactor: 10

# todo rr: passwords?
MinLenForNewPassword: 1

TokenTTL: "900h"
